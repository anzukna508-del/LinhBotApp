// File: build.gradle (Cấp Project)
plugins {
    // Khai báo phiên bản Android Gradle Plugin (AGP) ở đây
    // Version 8.2.0 chạy tốt với Gradle 8.6 mà bạn cài trong YAML
    id 'com.android.application' version '8.2.0' apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}